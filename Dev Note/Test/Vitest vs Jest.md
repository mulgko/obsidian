---
title: Vitest vs Jest
subject: "[[Test]]"
reference: ""
date: 2026-02-23 14:59
description: ""
tags:
  - vitest
  - jest
  - test
series: ""
seriesOrder:
published: false
---
# Vitest vs Jest — 상세 비교 가이드

---

## 목차

1. [개요](#1-개요)
2. [등장 배경 및 역사](#2-등장-배경-및-역사)
3. [핵심 차이점](#3-핵심-차이점)
4. [장단점 비교](#4-장단점-비교)
5. [설정 및 구성](#5-설정-및-구성)
6. [성능 비교](#6-성능-비교)
7. [API 호환성](#7-api-호환성)
8. [생태계 및 지원](#8-생태계-및-지원)
9. [어떨 때 무엇을 써야 하나?](#9-어떨-때-무엇을-써야-하나)
10. [마이그레이션 가이드](#10-마이그레이션-가이드)
11. [결론 요약](#11-결론-요약)

---

## 1. 개요

| 항목           | Jest                           | Vitest                           |
| -------------- | ------------------------------ | -------------------------------- |
| 개발사         | Meta (Facebook)                | Vite 팀 / Anthony Fu             |
| 출시           | 2014년                         | 2021년                           |
| 최신 주요 버전 | v29.x                          | v1.x / v2.x                      |
| 기반 번들러    | Babel / ts-jest                | Vite (esbuild)                   |
| 주요 사용처    | React, Node.js, CRA            | Vite 기반 프로젝트               |
| 공식 사이트    | [jestjs.io](https://jestjs.io) | [vitest.dev](https://vitest.dev) |

---

## 2. 등장 배경 및 역사

### Jest

- 2014년 Facebook이 오픈소스로 공개
- `jasmine` 기반으로 시작, 이후 자체 런타임으로 발전
- `create-react-app(CRA)` 기본 테스트 도구로 채택되며 폭발적으로 성장
- CommonJS 중심 설계, Babel 트랜스파일 의존
- Node.js 환경 테스트에 최적화
- 현재 JavaScript 생태계에서 가장 많이 사용되는 테스트 프레임워크

### Vitest

- 2021년 Vite 생태계의 확장으로 등장
- Vite 설정을 그대로 재사용하는 것이 핵심 철학
- esbuild 기반으로 매우 빠른 변환 속도
- ES Module(ESM) 네이티브 지원
- Jest API 호환을 목표로 설계 → 쉬운 마이그레이션
- Jotai, Pinia, Nuxt, SvelteKit 등 최신 프레임워크 생태계에서 급속도로 채택

---

## 3. 핵심 차이점

### 3.1 모듈 시스템

```
Jest: CommonJS (require/module.exports) 기본
       → ESM 지원은 --experimental-vm-modules 필요 (불완전)

Vitest: ES Module 네이티브 지원
         → import/export 문법 그대로 사용
         → CommonJS도 호환
```

**실무 영향**: 현대 프로젝트에서 ESM을 사용할 경우, Jest는 추가 설정이 복잡하고 버그가 잦음. Vitest는 별도 설정 없이 동작.

### 3.2 빌드 툴 연동

```
Jest:    Babel 또는 ts-jest로 트랜스파일
          → jest.config.js와 babel.config.js를 별도로 관리해야 함

Vitest:  Vite 설정(vite.config.ts)을 그대로 공유
          → alias, plugins, env 등이 자동으로 테스트 환경에도 적용됨
```

### 3.3 HMR / Watch 모드

```
Jest:   파일 변경 시 전체 테스트 재실행 (--watch 모드)
         → 여전히 느린 콜드 스타트

Vitest: Vite의 HMR 기반 스마트 감지
         → 변경된 모듈과 연관된 테스트만 선별 재실행
         → 체감 속도 훨씬 빠름
```

### 3.4 TypeScript 지원

|             | Jest                                                     | Vitest                             |
| ----------- | -------------------------------------------------------- | ---------------------------------- |
| 방식        | `ts-jest` 또는 `babel-jest` + `@babel/preset-typescript` | esbuild 기본 내장                  |
| 설정 복잡도 | 높음                                                     | 낮음                               |
| 타입 체킹   | `ts-jest`만 실제 타입 체크                               | 타입 체크 없이 트랜스파일만 (빠름) |
| 속도        | 느림                                                     | 매우 빠름                          |

> **주의**: Vitest는 기본적으로 TypeScript를 실행만 하고 타입 체크는 하지 않음. 타입 체크가 필요하면 별도로 `tsc --noEmit` 실행 필요.

### 3.5 실행 환경

```
Jest:   jsdom이 기본 (브라우저 환경 시뮬레이션)
Vitest: node가 기본, jsdom/happy-dom 선택 가능
```

### 3.6 병렬 실행 방식

|           | Jest                        | Vitest                         |
| --------- | --------------------------- | ------------------------------ |
| 기본 방식 | 파일별 별도 Worker 프로세스 | 파일별 쓰레드 (worker_threads) |
| 격리 수준 | 강한 격리 (별도 프로세스)   | 쓰레드 격리 (기본) 또는 포크   |
| 메모리    | 더 많이 사용                | 더 효율적                      |

---

## 4. 장단점 비교

### Jest

#### ✅ 장점

1. **검증된 안정성**: 10년 이상의 역사, 수많은 실전 사례
2. **방대한 생태계**: 플러그인, 문서, StackOverflow Q&A가 풍부
3. **올인원 패키지**: 모킹, 스냅샷, 코드 커버리지가 모두 내장
4. **Node.js 백엔드에 최적**: Express, NestJS, Fastify 등 서버 테스트에 강함
5. **기업 신뢰도**: Meta, Airbnb, Spotify 등 대기업 채택
6. **CRA/Next.js 기본 내장**: 별도 설정 없이 바로 사용 가능
7. **jsdom 기본 내장**: 브라우저 환경 시뮬레이션이 편리

#### ❌ 단점

1. **ESM 지원 불완전**: 네이티브 ESM 사용 시 복잡한 추가 설정 필요
2. **느린 초기 실행**: 특히 TypeScript 프로젝트에서 콜드 스타트가 느림
3. **Vite 생태계와 괴리**: Vite 설정과 Jest 설정을 별도로 유지해야 함
4. **설정 파일이 복잡**: `jest.config.js` + `babel.config.js` + `tsconfig.json` 관리 부담
5. **모킹 문법이 verbose**: `jest.mock()`, `jest.spyOn()` 등이 다소 장황

---

### Vitest

#### ✅ 장점

1. **압도적인 속도**: esbuild 기반으로 번들 및 변환이 매우 빠름
2. **Vite 설정 재사용**: `vite.config.ts` 하나로 앱 + 테스트 설정 통합
3. **ESM 네이티브**: 별도 설정 없이 `import/export` 사용
4. **직관적인 TypeScript**: 별도 ts-jest 없이 즉시 동작
5. **Jest API 호환**: `describe`, `it`, `expect`, `vi.mock()` 등 유사한 API
6. **스마트 Watch 모드**: 변경된 파일과 연관된 테스트만 선별 실행
7. **UI 모드**: `--ui` 플래그로 테스트 결과를 브라우저에서 시각화
8. **In-source testing**: 소스 파일 내부에 바로 테스트 작성 가능
9. **happy-dom 지원**: jsdom보다 빠른 DOM 환경 제공

#### ❌ 단점

1. **Vite 의존성**: Vite를 사용하지 않는 프로젝트에서는 Vite를 추가로 설치해야 함
2. **상대적으로 짧은 역사**: 2021년 출시, 일부 엣지 케이스 버그 가능성
3. **생태계 크기**: Jest에 비해 서드파티 플러그인, 문서가 적음
4. **타입 체크 없음**: 기본적으로 타입 오류를 테스트 실패로 잡지 않음
5. **Node.js 전용 프로젝트에서 오버엔지니어링**: 순수 Node 백엔드에서는 굳이 Vite 기반 도구를 쓸 필요 없음
6. **일부 Jest 플러그인 미지원**: `jest-extended`, 일부 커스텀 reporter 등

---

## 5. 설정 및 구성

### Jest 설정 예시 (TypeScript 프로젝트)

```javascript
// jest.config.js
module.exports = {
  preset: "ts-jest",
  testEnvironment: "node",
  roots: ["<rootDir>/src"],
  testMatch: ["**/__tests__/**/*.ts", "**/*.test.ts"],
  moduleNameMapper: {
    "^@/(.*)$": "<rootDir>/src/$1", // alias 설정
  },
  collectCoverageFrom: ["src/**/*.ts"],
  setupFilesAfterFramework: ["./jest.setup.ts"],
};
```

```json
// package.json (별도 Babel 설정도 필요할 수 있음)
{
  "devDependencies": {
    "jest": "^29.0.0",
    "ts-jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}
```

### Vitest 설정 예시 (Vite 프로젝트)

```typescript
// vite.config.ts (앱 설정과 테스트 설정이 하나로!)
import { defineConfig } from "vitest/config";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: { "@": "/src" }, // 앱과 테스트에서 모두 사용됨
  },
  test: {
    environment: "jsdom",
    globals: true, // describe, it, expect 전역 사용
    setupFiles: ["./src/test/setup.ts"],
    coverage: {
      provider: "v8",
      reporter: ["text", "html"],
    },
  },
});
```

```json
// package.json
{
  "devDependencies": {
    "vitest": "^2.0.0",
    "@vitest/ui": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0"
  },
  "scripts": {
    "test": "vitest",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest run --coverage"
  }
}
```

---

## 6. 성능 비교

> 아래 수치는 일반적인 중간 규모 프로젝트(~100개 테스트 파일) 기준 대략적 비교

| 시나리오                   | Jest     | Vitest    |
| -------------------------- | -------- | --------- |
| 콜드 스타트 (첫 실행)      | ~15-30초 | ~3-8초    |
| Watch 모드 (파일 1개 변경) | ~5-10초  | ~0.5-2초  |
| TypeScript 대규모 프로젝트 | 느림     | 빠름      |
| 순수 JS 소규모 프로젝트    | 보통     | 약간 빠름 |

**이유**: Vitest는 esbuild로 트랜스파일하고 Vite의 의존성 캐싱을 활용함. Jest는 Babel/ts-jest가 파일마다 트랜스파일을 수행하며 상대적으로 무거움.

---

## 7. API 호환성

Vitest는 Jest API와 거의 동일하게 사용할 수 있도록 설계됨.

### 공통 API (거의 동일)

```typescript
// 둘 다 동일하게 동작
describe("Calculator", () => {
  it("should add numbers", () => {
    expect(add(1, 2)).toBe(3);
    expect(add(1, 2)).toEqual(3);
    expect(fetchData()).resolves.toBe("data");
  });

  it("should throw on invalid input", () => {
    expect(() => divide(1, 0)).toThrow("Division by zero");
  });
});
```

### 모킹 차이점

```typescript
// Jest
import { myModule } from "./myModule";
jest.mock("./myModule");
const mockFn = jest.fn();
jest.spyOn(object, "method");

// Vitest
import { myModule } from "./myModule";
vi.mock("./myModule");
const mockFn = vi.fn();
vi.spyOn(object, "method");
```

→ `jest` → `vi` 로만 바꾸면 대부분 동작

### 타이머 모킹

```typescript
// Jest
jest.useFakeTimers();
jest.advanceTimersByTime(1000);
jest.useRealTimers();

// Vitest
vi.useFakeTimers();
vi.advanceTimersByTime(1000);
vi.useRealTimers();
```

### 스냅샷 테스트

두 프레임워크 모두 동일한 문법으로 지원:

```typescript
expect(component).toMatchSnapshot();
expect(value).toMatchInlineSnapshot(`"hello"`);
```

---

## 8. 생태계 및 지원

### Jest 생태계

- **@testing-library/jest-dom**: DOM assertion 확장
- **jest-extended**: 추가 matcher 모음
- **jest-mock-extended**: TypeScript 강화 모킹
- **jest-circus**: 기본 테스트 러너
- **jest-runner-eslint**: ESLint를 Jest로 실행
- **msw**: API 모킹 (Jest와 완벽 호환)

### Vitest 생태계

- **@vitest/ui**: 브라우저 기반 테스트 UI
- **@vitest/coverage-v8**: V8 기반 코드 커버리지
- **@vitest/coverage-istanbul**: Istanbul 기반 코드 커버리지
- **@testing-library/\***: 대부분 Vitest와 호환
- **msw**: API 모킹 (Vitest와도 호환)
- **happy-dom**: jsdom보다 빠른 DOM 환경

---

## 9. 어떨 때 무엇을 써야 하나?

### ✅ Vitest를 선택해야 할 때

| 상황                                                        | 이유                       |
| ----------------------------------------------------------- | -------------------------- |
| **Vite 기반 프로젝트** (React+Vite, Vue, Svelte, SvelteKit) | 설정 공유로 즉시 사용 가능 |
| **Nuxt 3**                                                  | 공식 권장 테스트 도구      |
| **최신 ES Module 프로젝트**                                 | ESM 네이티브 지원          |
| **개발 속도가 최우선**                                      | HMR Watch 모드가 매우 빠름 |
| **TypeScript 중심 프로젝트**                                | ts-jest 없이 바로 동작     |
| **테스트 수가 많고 CI 속도가 중요할 때**                    | 번들 성능 우위             |
| **소규모~중간 규모 신규 프로젝트**                          | 설정 부담이 낮음           |

### ✅ Jest를 선택해야 할 때

| 상황                                               | 이유                           |
| -------------------------------------------------- | ------------------------------ |
| **레거시 프로젝트 유지보수**                       | 기존 설정 변경 비용 최소화     |
| **Create React App (CRA)**                         | 기본 내장, 추가 설정 불필요    |
| **Next.js (Pages Router)**                         | 공식 Jest 권장 설정 제공       |
| **순수 Node.js 백엔드** (Express, NestJS, Fastify) | Vite 없이 깔끔하게 사용 가능   |
| **대규모 엔터프라이즈 팀**                         | 검증된 안정성, 풍부한 레퍼런스 |
| **복잡한 모킹이 많은 프로젝트**                    | 더 성숙한 모킹 생태계          |
| **Jest 플러그인 의존성이 높을 때**                 | jest-extended 등 전용 플러그인 |

### 📌 실전 결정 흐름

```
신규 프로젝트인가?
  ├─ YES → Vite를 사용하는가?
  │         ├─ YES → ✅ Vitest
  │         └─ NO  → Node.js 백엔드인가?
  │                   ├─ YES → ✅ Jest
  │                   └─ NO  → Vite 도입 고려 후 Vitest
  │
  └─ NO (기존 프로젝트)
            ├─ Jest 사용 중 → ✅ 유지 (마이그레이션 비용 고려)
            ├─ Vite로 이미 전환 → ✅ Vitest로 마이그레이션 고려
            └─ 테스트가 아예 없음 → 스택에 따라 선택
```

---

## 10. 마이그레이션 가이드

### Jest → Vitest 마이그레이션

**1단계: 패키지 변경**

```bash
# 제거
npm uninstall jest @types/jest ts-jest babel-jest

# 설치
npm install -D vitest @vitest/coverage-v8
```

**2단계: 설정 파일 변환**

```typescript
// jest.config.js를 vite.config.ts의 test 블록으로 이동
export default defineConfig({
  test: {
    globals: true, // jest globals 사용 시 필요
    environment: "jsdom", // testEnvironment: 'jsdom' 대응
    setupFiles: ["./jest.setup.ts"], // setupFilesAfterFramework 대응
  },
});
```

**3단계: import 변경**

```typescript
// Before (Jest)
import { jest } from "@jest/globals";

// After (Vitest)
import { vi } from "vitest";
// 또는 globals: true 설정 시 vi를 전역으로 사용 가능
```

**4단계: jest → vi 치환**

```bash
# 코드 일괄 치환
sed -i 's/jest\./vi\./g' src/**/*.test.ts
sed -i 's/jest\.mock/vi\.mock/g' src/**/*.test.ts
```

**5단계: package.json 스크립트 변경**

```json
{
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  }
}
```

---

## 11. 결론 요약

```
┌─────────────────────────────────────────────────────────┐
│                     결론 한눈에 보기                       │
├─────────────────┬───────────────────────────────────────┤
│ 항목            │ 추천                                    │
├─────────────────┼───────────────────────────────────────┤
│ 동작 속도       │ Vitest 우세 (특히 TS, 대규모)           │
│ 설정 편의성     │ Vitest (Vite 사용 시), Jest (Node)      │
│ 생태계 성숙도   │ Jest 우세                               │
│ ESM 지원        │ Vitest 우세                             │
│ 안정성          │ Jest 우세                               │
│ 현대 프레임워크 │ Vitest 우세 (Vue, Svelte, Nuxt 등)      │
│ 레거시 프로젝트 │ Jest 유지 권장                          │
│ 학습 곡선       │ 거의 동일 (API 호환)                    │
└─────────────────┴───────────────────────────────────────┘
```

**한 줄 요약**:

- **Vite 기반 현대 프로젝트** → **Vitest**
- **Node.js 백엔드 / 레거시 프로젝트** → **Jest**
- **둘 다 고려된다면** → 현재 사용 중인 빌드 툴을 따라가는 것이 최선

---

_참고 자료_

- [Vitest 공식 문서](https://vitest.dev)
- [Jest 공식 문서](https://jestjs.io)
- [Vitest vs Jest 마이그레이션 가이드](https://vitest.dev/guide/migration.html)
